import React from "react";

const Demo = ({ handleprops }) => {
  return (
    <div>
      <button className="btn" onClick={handleprops}>
        Testty
      </button>
    </div>
  );
};

export default Demo;
